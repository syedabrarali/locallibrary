extends layout 

block content 
  h1= title

  ul 
    each author in author_list 
      li 
        a(href=author.url) !{author.name} 
        | (#{author.f_DOB} - #{author.f_DOD})
          

    else 
      h2 There are no Authors list available  